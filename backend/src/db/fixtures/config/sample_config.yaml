yagna:
  #  api_url: "http://127.0.0.1:6765"
  #  gsb_url: "tcp://127.0.0.1:6766"
  app_key: "$YAGNA_APPKEY"
  subnet_tag: "public"

payment:
  budget: 1.0
  driver: "erc20"
  network: "rinkeby"

limits:
  startup_timeout: 300 # timeout for the provisioning of an app (seconds)
  max_running_time: 3600 # the maximum running time for an app (seconds)
